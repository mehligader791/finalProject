// Generate a random TypeScript code snippet based on the given requirements

/**
 * This program is designed to demonstrate how to create and manipulate arrays in TypeScript.
 * It also includes some basic error handling and variable initialization techniques.

 // Example usage:
 const array = [1, 2, 3];
 console.log(array); // Output: [1, 2, 3]

 try {
   const object = { name: "John", age: 30 };
 } catch (error) {
   console.error(error.message);
 }

 // Array manipulation with loops
 let arrayToBeManipulated = [1, 2, 3, 4];
 for (let i = 0; i < arrayToBeManipulated.length; i++) {
   arrayToBeManipulated[i] *= 2;
 }
 console.log(arrayToBeManipulated); // Output: [2, 4, 6, 8]

 try {
   arrayToBeManipulated.push(5);
 } catch (error) {
   console.error(error.message);
 }
 console.log(arrayToBeManipulated); // No error output
